[project]
name = "canton8-hack"
version = "0.1.0"
description = "A new project scaffold for canton8_hack"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Your Name" }]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
